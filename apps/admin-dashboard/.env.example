# Accelerator control plane (server-side only; proxied via Nuxt server routes)
NANCE_ACCELERATOR_URL=http://localhost:8080

# Optional: must match control plane NANCE_ADMIN_TOKEN if set
NANCE_ADMIN_TOKEN=